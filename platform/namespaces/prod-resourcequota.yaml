apiVersion: v1
kind: ResourceQuota
metadata:
  name: prod-quota
  namespace: prod
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  hard:
    pods: "100"
    requests.cpu: "10"
    requests.memory: 16Gi
    limits.cpu: "20"
    limits.memory: 32Gi