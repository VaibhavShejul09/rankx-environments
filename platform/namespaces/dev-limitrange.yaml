apiVersion: v1
kind: LimitRange
metadata:
  name: dev-default-limits
  namespace: dev
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  limits:
  - default:
      cpu: 500m
      memory: 512Mi
    defaultRequest:
      cpu: 200m
      memory: 256Mi
    type: Container