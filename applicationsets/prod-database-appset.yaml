apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: prod-databases
  namespace: argocd
spec:
  generators:
  - git:
      repoURL: https://github.com/VaibhavShejul09/rankx-environments.git
      revision: main
      directories:
      - path: environments/prod/databases/*
  template:
    metadata:
      name: '{{path.basename}}-prod'
      labels:
        type: database
        env: prod
      annotations:                     # 🔥 ADD HERE
        argocd.argoproj.io/sync-wave: "2"  
    spec:
      project: databases
      source:
        repoURL: https://github.com/VaibhavShejul09/centralized-helm-repo.git
        targetRevision: main
        path: charts/microservice-base
        helm:
          valueFiles:
          - values.yaml
      destination:
        server: https://kubernetes.default.svc
        namespace: prod
      syncPolicy:
        automated:
          prune: false
          selfHeal: false
        retry:
          limit: 3
          backoff:
              duration: 5s
              factor: 2
              maxDuration: 1m  
        syncOptions:
        - CreateNamespace=true   